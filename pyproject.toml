[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "crowdmath2026"
version = "0.1.0"
description = "Computational experiments for MIT PRIMES CrowdMath 2026 — arithmetic of rational semirings"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
